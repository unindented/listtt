import { Theme } from "./types";

const theme: Theme = {
  statusBar: "dark-content",
  colors: {
    bodyBackground: "#fff",
    bodyText: "#303942",
    bodySubtext: "#808080",
  },
};

export default theme;
